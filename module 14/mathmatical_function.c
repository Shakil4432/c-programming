#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{
    // double a;
    // scanf("%lf", &a);
    // int c = ceil(a);
    // printf("%d", c);

    // double a;
    // scanf("%lf", &a);
    // int f = floor(a);
    // printf("%d", f);

    // double a;
    // scanf("%lf", &a);
    // int r = round(a);
    // printf("%d", r);

    // double a;
    // scanf("%lf", &a);
    // double s = sqrt(a);
    // printf("%0.2lf", s);

    double a;
    scanf("%lf", &a);
    double s = abs(a);
    printf("%0.2lf", s);
    return 0;
}